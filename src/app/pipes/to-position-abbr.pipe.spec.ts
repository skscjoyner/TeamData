import { ToPositionAbbrPipe } from './to-position-abbr.pipe';

describe('ToPositionAbbrPipe', () => {
  it('create an instance', () => {
    const pipe = new ToPositionAbbrPipe();
    expect(pipe).toBeTruthy();
  });
});
